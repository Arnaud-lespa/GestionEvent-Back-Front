import { NodeCompatibleEventEmitter } from "rxjs/internal/observable/fromEvent";

export interface IRegister {
    id : number 
    pseudo : string
    email : string
    firstname : string
    lastname : string
    birthdate : string
    password : string
}